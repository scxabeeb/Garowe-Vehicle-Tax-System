@page "{id:int}"
@model VehicleTax.Web.Pages.CarTypes.EditModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h4 class="mb-0">✏️ Edit Car Type</h4>
                </div>

                <div class="card-body">
                    <form method="post">

                        <div asp-validation-summary="All" class="text-danger mb-3"></div>

                        <input type="hidden" asp-for="CarType.Id" />

                        <div class="mb-3">
                            <label class="form-label">Car Type Name</label>
                            <input asp-for="CarType.Name" class="form-control" />
                            <span asp-validation-for="CarType.Name" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button class="btn btn-success">💾 Update</button>
                            <a asp-page="Index" class="btn btn-outline-secondary">⬅ Back</a>
                        </div>

                    </form>
                </div>
            </div>

        </div>
    </div>
</div>
