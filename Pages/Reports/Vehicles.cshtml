@page
@model VehicleTax.Web.Pages.Reports.VehiclesModel
@{
    ViewData["Title"] = "Vehicle Report";
}

<h3 class="fw-bold mb-4">Vehicle Report</h3>

<form method="get" class="card p-3 shadow-sm mb-4">
    <div class="row g-3">

        <div class="col-md-2">
            <label class="form-label">Plate Number</label>
            <input asp-for="PlateNumber" class="form-control" />
        </div>

        <div class="col-md-2">
            <label class="form-label">Owner Name</label>
            <input asp-for="OwnerName" class="form-control" />
        </div>

        <div class="col-md-2">
            <label class="form-label">Mobile</label>
            <input asp-for="Mobile" class="form-control" />
        </div>

        <div class="col-md-2">
            <label class="form-label">Car Type</label>
            <select asp-for="CarTypeId" asp-items="Model.CarTypes" class="form-select">
                <option value="">All</option>
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Show</label>
            <select asp-for="PageSize" class="form-select">
                <option value="10">10</option>
                <option value="20">20</option>
                <option value="50">50</option>
                <option value="100">100</option>
                <option value="-1">All</option>
            </select>
        </div>

        <div class="col-md-1 d-flex align-items-end">
            <button class="btn btn-primary w-100">Filter</button>
        </div>

        <div class="col-md-1 d-flex align-items-end">
            <a asp-page-handler="ExportCsv"
               asp-route-plateNumber="@Model.PlateNumber"
               asp-route-ownerName="@Model.OwnerName"
               asp-route-mobile="@Model.Mobile"
               asp-route-carTypeId="@Model.CarTypeId"
               class="btn btn-success w-100">
                CSV
            </a>
        </div>

    </div>
</form>

<div class="row mb-3">
    <div class="col-md-4">
        <div class="card text-center shadow-sm">
            <div class="card-body">
                <h6>Total Vehicles</h6>
                <h4>@Model.TotalRecords</h4>
            </div>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Plate</th>
            <th>Owner</th>
            <th>Mobile</th>
            <th>Car Type</th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Vehicles.Any())
        {
            <tr>
                <td colspan="4" class="text-center text-muted">
                    No vehicles found for the selected filters.
                </td>
            </tr>
        }
        else
        {
            foreach (var v in Model.Vehicles)
            {
                <tr>
                    <td>@v.PlateNumber</td>
                    <td>@v.OwnerName</td>
                    <td>@v.Mobile</td>
                    <td>@v.CarType?.Name</td>
                </tr>
            }
        }
    </tbody>
</table>

@if (Model.PageSize != -1 && Model.TotalPages > 1)
{
    <nav>
        <ul class="pagination justify-content-center">

            <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-route-PageNumber="@(Model.PageNumber - 1)"
                   asp-route-PageSize="@Model.PageSize"
                   asp-route-PlateNumber="@Model.PlateNumber"
                   asp-route-OwnerName="@Model.OwnerName"
                   asp-route-Mobile="@Model.Mobile"
                   asp-route-CarTypeId="@Model.CarTypeId">
                    Previous
                </a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                    <a class="page-link"
                       asp-route-PageNumber="@i"
                       asp-route-PageSize="@Model.PageSize"
                       asp-route-PlateNumber="@Model.PlateNumber"
                       asp-route-OwnerName="@Model.OwnerName"
                       asp-route-Mobile="@Model.Mobile"
                       asp-route-CarTypeId="@Model.CarTypeId">
                        @i
                    </a>
                </li>
            }

            <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")">
                <a class="page-link"
                   asp-route-PageNumber="@(Model.PageNumber + 1)"
                   asp-route-PageSize="@Model.PageSize"
                   asp-route-PlateNumber="@Model.PlateNumber"
                   asp-route-OwnerName="@Model.OwnerName"
                   asp-route-Mobile="@Model.Mobile"
                   asp-route-CarTypeId="@Model.CarTypeId">
                    Next
                </a>
            </li>

        </ul>
    </nav>
}
