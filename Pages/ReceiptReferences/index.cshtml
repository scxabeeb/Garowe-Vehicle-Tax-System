@page
@model VehicleTax.Web.Pages.ReceiptReferences.IndexModel
@{
    ViewData["Title"] = "Receipt References";
}

<<<<<<< HEAD
<style>
    .pagination-scroll {
        overflow-x: auto;
        white-space: nowrap;
        padding-bottom: 6px;
    }

    .pagination-scroll .pagination {
        flex-wrap: nowrap;
    }

    .pagination-scroll::-webkit-scrollbar {
        height: 6px;
    }

    .pagination-scroll::-webkit-scrollbar-thumb {
        background: #ccc;
        border-radius: 10px;
    }
</style>

=======
>>>>>>> 7b24e7a6af078c71b19b1652f4befd2904f706dd
<div class="container mt-4">

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- HEADER -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h3 class="fw-bold mb-0">Receipt References</h3>
<<<<<<< HEAD
            <small class="text-muted">Manage receipt numbers used for payments</small>
        </div>

        <div>
            <a asp-page="Create" class="btn btn-primary me-2">Add New</a>
            <a asp-page="Upload" class="btn btn-outline-secondary">Upload CSV</a>
        </div>
    </div>

    <!-- SEARCH + PAGE SIZE -->
    <form method="get" class="row g-2 mb-3">

        <div class="col-md-5">
=======
            <small class="text-muted">
                Manage receipt numbers used for payments
            </small>
        </div>

        <div>
            <a asp-page="Create" class="btn btn-primary me-2">
                Add New
            </a>

            <a asp-page="Upload" class="btn btn-outline-secondary">
                Upload CSV
            </a>
        </div>
    </div>

    <!-- SEARCH -->
    <form method="get" class="row g-2 mb-3">
        <div class="col-md-6">
>>>>>>> 7b24e7a6af078c71b19b1652f4befd2904f706dd
            <input name="Search"
                   value="@Model.Search"
                   class="form-control"
                   placeholder="Search receipt number..." />
        </div>

        <div class="col-md-3">
<<<<<<< HEAD
            <select name="PageSize" class="form-select" onchange="this.form.submit()">
                <option value="10"  selected="@(Model.PageSize==10)">10 per page</option>
                <option value="20"  selected="@(Model.PageSize==20)">20 per page</option>
                <option value="50"  selected="@(Model.PageSize==50)">50 per page</option>
                <option value="100" selected="@(Model.PageSize==100)">100 per page</option>
                <option value="200" selected="@(Model.PageSize==200)">200 per page</option>
                <option value="300" selected="@(Model.PageSize==300)">300 per page</option>
                <option value="500" selected="@(Model.PageSize==500)">500 per page</option>
                <option value="0"   selected="@(Model.PageSize==0)">All</option>
            </select>
        </div>

        <div class="col-md-2">
            <button class="btn btn-outline-primary w-100">Search</button>
        </div>

    </form>

    <!-- PAGINATION (SCROLLABLE) -->
    @if (Model.TotalPages > 1)
    {
        <nav class="mb-2">
            <div class="pagination-scroll">
                <ul class="pagination pagination-sm mb-0">

                    <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                        <a class="page-link"
                           href="?pageNumber=@(Model.PageNumber-1)&search=@Model.Search&pageSize=@Model.PageSize">
                            Prev
                        </a>
                    </li>

                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                            <a class="page-link"
                               href="?pageNumber=@i&search=@Model.Search&pageSize=@Model.PageSize">
                                @i
                            </a>
                        </li>
                    }

                    <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")">
                        <a class="page-link"
                           href="?pageNumber=@(Model.PageNumber+1)&search=@Model.Search&pageSize=@Model.PageSize">
                            Next
                        </a>
                    </li>

                </ul>
            </div>
=======
            <button class="btn btn-outline-primary w-100">
                Search
            </button>
        </div>
    </form>

    <!-- PAGINATION ABOVE TABLE -->
    @if (Model.TotalPages > 1)
    {
        <nav class="mb-2">
            <ul class="pagination pagination-sm justify-content-center mb-0">
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                        <a class="page-link"
                           href="?pageNumber=@i&search=@Model.Search">
                            @i
                        </a>
                    </li>
                }
            </ul>
>>>>>>> 7b24e7a6af078c71b19b1652f4befd2904f706dd
        </nav>
    }

    <!-- TABLE -->
    <div class="card shadow-sm border-0">
        <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle mb-0">

                <thead class="table-light">
                    <tr>
                        <th>Receipt Number</th>
                        <th style="width:140px" class="text-center">Status</th>
                        <th style="width:130px" class="text-center">Actions</th>
                    </tr>
                </thead>

                <tbody>
                @if (Model.Receipts.Any())
                {
                    foreach (var r in Model.Receipts)
                    {
                        <tr class="@(r.IsCancelled ? "table-secondary text-muted" : "")">
                            <td class="fw-semibold">@r.ReferenceNumber</td>

                            <td class="text-center">
                                @if (r.IsCancelled)
                                {
                                    <span class="badge bg-dark">Cancelled</span>
                                }
                                else if (r.IsUsed)
                                {
                                    <span class="badge bg-success">Used</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Available</span>
                                }
                            </td>

                            <td class="text-center">
                                <a asp-page="View"
                                   asp-route-id="@r.Id"
                                   class="btn btn-sm btn-outline-info">
                                    View
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted py-4">
                            No receipt references found
                        </td>
                    </tr>
                }
                </tbody>

            </table>
        </div>
    </div>

</div>
